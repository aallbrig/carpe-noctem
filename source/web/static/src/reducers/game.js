const initialState = {
  height: 540,
  width: 540
};
const Game = () => (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
};
module.exports = {
  Game
};
